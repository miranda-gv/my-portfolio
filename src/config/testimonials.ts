/**
 * Testimonials Display Configuration
 *
 * Controls how testimonials are grouped into frames (pages) for display.
 * - defaultPerFrame: Number of testimonials shown per frame
 */
export const testimonialConfig = {
  defaultPerFrame: 3,
} as const;
